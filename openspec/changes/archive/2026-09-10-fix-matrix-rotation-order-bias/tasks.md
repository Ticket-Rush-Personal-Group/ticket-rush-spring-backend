> **只改 `k6/run-comparison-matrix.sh` 一個檔案。** 不改應用程式碼、不改量測參數、
> 不改 `run-load-test.sh`、不改組別定義。
> **本支不需要跑任何量測** —— 驗證全部由 `MATRIX_RESOLVE_ONLY` 完成。
>
> 塊的依賴關係:
> - **塊 1(均衡檢查)必須排在塊 2(排程修正)之前。**
>   順序顛倒的話檢查從頭到尾都是綠的,而那與「檢查沒作用」無法區分。
>   這與第 14 支的 D4 是同一個道理。

## 1. 均衡檢查(必須先做,在舊排程上)

- [x] 1.1 擴充 `MATRIX_RESOLVE_ONLY`:印出**各輪的完整組別順序**
- [x] 1.2 逐配對統計先後次數,輸出最小值與最大值
- [x] 1.3 任一配對超出 `floor(R/2)`～`ceil(R/2)` 即**明確標示排程有缺陷**,
      並列出違規的配對
- [x] 1.4 **驗證(反向,在舊的旋轉排程上做)**:
      六組三輪應報出 `P200_pessimistic` / `P1000_pessimistic` 為 **3:0**;
      八組三輪同樣應報出缺陷。**這一步做完才能進塊 2**

      舊排程六組三輪報出 **3 個失衡配對**,其中一個正是預測的
      `P200_pessimistic 先於 P1000_pessimistic 共 3/3 輪` ——
      **與第 15 支 log 的實際順序完全吻合**。另外兩個
      (`V_pessimistic`/`P200_noLock`、`P1000_noLock`/`V_noLock`)是我手算時沒發現的。

      **離開碼也驗過:** 以暫存副本還原成舊排程 → `exit 1`;新排程 → `exit 0`。
      (改壞用的腳本帶 `assert old in s`,沒改到就讓測試自己失敗。)
- [x] 1.5 **驗證**:`bash -n` 通過

## 2. 排程改為「反序配對 + 正向旋轉」

- [x] 2.1 偶數輪 = 其前一輪的**完整反序**;奇數輪 = 正向旋轉
- [x] 2.2 **不是「旋轉後再反序」** —— 那會把一部分配對從 2:1 弄成 3:0(見 design D1)
- [x] 2.3 正向輪之間的偏移由**正向輪的序號**決定:
      `正向輪數 = ceil(R/2)`,第 m 個正向輪的偏移 = `(m-1) × 組數 / 正向輪數`
- [x] 2.4 輪次標題要標明該輪是正向(含偏移)還是反序
- [x] 2.5 **驗證**:`bash -n` 通過

## 3. 驗證新排程

- [x] 3.1 六組三輪:所有配對皆為 2:1 或 1:2,檢查通過
- [x] 3.2 八組三輪(**預設路徑**):同上 ——
      第 13 支的教訓:帶參數驗證過不等於預設路徑驗證過
- [x] 3.3 兩組兩輪(小規模驗證用的組態):應為 1:1,**完全平衡**
- [x] 3.4 確認絕對位置仍分散 —— **反序不得把旋轉原本解決的問題弄回來**

      八組三輪:**每一組都落在三個相異位置**(最少相異位置數 3 / 3)。
      位置那一半也做成可檢查的輸出,不再靠眼睛看。
      六組 15 配對、八組 28 配對、兩組兩輪 1 配對,**全部在允許範圍內**;
      兩組兩輪為 1:1,完全平衡(輪數為偶數時可達)。
- [x] 3.5 確認組別解析、漂移門檻、耗時上限、電源守則等既有行為未受影響

      跑一次真的小規模量測(兩組兩輪)確認主迴圈:
      第 2 輪確實是完整反序(V 先、P 後),image 建置、電源守則、
      准入上限自報、app 記憶體、漂移判定全部照常。漂移 3.2%,批次有效。

## 4. 收尾

- [x] 4.1 `tasks/todo.md`:更正「保留循環相鄰關係」的用詞 ——
      **真正的缺陷是「順序固定」,不是「相鄰」**。相鄰本身無害,
      只要三輪裡有時 A 先、有時 B 先,趨勢的影響就會抵銷
- [x] 4.2 `tasks/todo.md`:第 14 支八組表與第 15 支六組數據各補一句
      「由舊排程產生,已知有先後固定的偏差」——
      **讓讀者知道限制,而不是由我判斷它「應該沒差」**
- [x] 4.3 `tasks/lessons.md`:**只在真的踩到坑時才寫**。
      候選:「保序的變換不會消除順序偏差」——**只在它不只是本專案的實作細節時才寫**

      **寫。** 它不是實作細節,是一個撐了兩支 change 的誤解:
      **旋轉在視覺上很像「打亂」,而且位置那一半是真的解決了,所以檢查位置會通過** ——
      先後那一半完全沒有徵兆。

      而且**我的第一個修法(偶數輪反轉)也是錯的,錯得很像對的** ——
      它把一部分配對修好、同時把另一部分弄壞,若只檢查原本壞掉的那個配對會以為修好了。
      那個「我以為 X,實際是 Y」正是本專案 lessons 要記的東西。
- [x] 4.4 評估是否有新守則該進 `CLAUDE.md` 的 Hard Rules ——
      **只在確實可被檢查、且既有規則未涵蓋時才寫**

      **不寫。** 逐條檢查:

      | 候選 | 為什麼不寫 |
      | --- | --- |
      | 輪換要同時均衡位置與先後 | 已是 spec 需求 —— **專案慣例歸 openspec,不歸 CLAUDE.md** |
      | 下結論前排程要通過均衡檢查 | `MATRIX_RESOLVE_ONLY` 已會回非零 ——**機器抓得到就不需要人記** |
      | 比較數據必須出自矩陣腳本 | **既有 Hard Rule 已涵蓋**(第 13 支加的那條) |

      連續第三支得到「不加」的結論,而那是好事:**Hard Rules 該長得很慢。**
- [x] 4.5 完整驗證鏈:`compile` / `spotless:check` / `verify` 三者皆綠
      (本支未動 Java,但仍須確認沒有誤傷)

      三者皆綠,**147 個測試 0 失敗**。本支未動 Java,測試數與前三支相同。
- [x] 4.6 需要使用者手動執行的動作:commit、push、archive 指令
- [ ] 4.7 archive 這支 change(`openspec archive fix-matrix-rotation-order-bias -y`)
