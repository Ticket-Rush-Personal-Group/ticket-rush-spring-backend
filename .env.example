# 開發環境的資料庫密碼。
#
# 值與 ~/dev-databases/.env 的 POSTGRES_PASSWORD 相同 —— 開發時連的是那組共用容器
# (postgres:17 on 5432),本專案不另起開發用資料庫。
#
# 整合測試不需要這個變數:Testcontainers 會自行啟動 postgres:17 容器,
# 並由 @ServiceConnection 覆蓋 application.yml 的 datasource 設定。
#
# 初次設定需先建立 schema:
#   docker exec my-postgres createdb -U postgres ticket_rush_db
DEV_DB_PASSWORD=
