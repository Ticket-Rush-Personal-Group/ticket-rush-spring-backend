/**
 * 領域模型:Event、Stock、Order 等聚合與實體。
 *
 * <p>不得出現任何框架的 annotation 或 import(Spring、JPA、Jackson)。領域對「使用何種
 * 併發控制策略」一無所知 —— 這是「四種策略切換、domain 零改動」這項主張的技術基礎。
 */
package com.alantsai.ticketrush.domain.model;
