# Todo

> 跨 change 的待辦、需決定事項與延後功能。

## Phase 1 的 change 規劃

依序執行,每支一條 branch(從 `develop` 切)。切塊標準是能否獨立通過驗證鏈。

- [x] 1. `add-project-skeleton` —— **完成**。Maven + Spring Boot 4.1.1 + 六角目錄(14 個 package)+ ArchUnit guardrail(4 個 `@ArchTest`,各自經反向驗證)+ Spotless。`./mvnw verify` 綠,6 個測試通過
- [x] 2. `add-domain-model` —— **完成**。Flyway V1 三張表、domain model 與 value object、JPA entity 與手寫 mapper、三個基本 out port 與 persistence adapter、Testcontainers 整合測試。從第 1 支延後的兩條 ArchUnit 規則已補上並各自反向驗證(共 7 條規則)。`./mvnw verify` 綠,37 個測試
- [x] 3. `add-purchase-api-no-lock` —— **完成**。購票 API、`PurchaseFacade` 與策略選擇、第 0 層無鎖實作、統一 API wrapper 與錯誤映射、最後一條延後的 ArchUnit 規則(共 8 條)。`./mvnw verify` 綠,55 個測試。
      **超賣證據已取得:1000 併發搶 500 張,售出 1000 張、庫存只減少 144,超賣 856 張**(READ COMMITTED、未套資源限制)。這是 README 的開場數據
- [x] 4. `add-load-test-harness` —— **完成**(分支 `feat/add-load-test-harness`)。Dockerfile + compose `perf` profile + k6 腳本 + RuntimeInfoLogger。資源限制經反向驗證確實生效(cpus 4→2 時 JVM 報告跟著變)。第 0 層基準數據已取得:平台執行緒 p99 932.59ms / 超賣 862 張,虛擬執行緒 p99 872.88ms / 超賣 884 張
- [x] 5. `add-purchase-limit-policy` —— **完成**(分支 `feat/add-purchase-limit-policy`)。一人一場限購上限。限購規則置於 domain 且可純單元測試驗證,檢查順序為先限購後庫存。`./mvnw verify` 綠,82 個測試。**第二組證據已取得:20 個併發請求下超買 6 張**(上限 4)
- [x] 6. `add-pessimistic-lock-strategy` —— **完成**。第一個正確的實作。限購採「鎖前快篩 + 鎖後權威檢查」雙重檢查。`./mvnw verify` 綠,85 個測試(含兩個併發正確性**驗收**測試)。**零超賣、零超買;吞吐 693.76 req/s(無鎖同條件 827.58,-16%)。連線池對照判定瓶頸在連線池而非鎖(10→50 提升 36%)**
- [x] 7. `add-optimistic-lock-strategy` —— **完成**。條件式 UPDATE(`WHERE version = ?`)偵測衝突,
      重試迴圈置於交易之外(兩個 bean,避免 self-invocation)。讀取順序「先版本、後已購數」是正確性的一部分。
      `./mvnw verify` 綠,110 個測試。**零超賣、零超買;吞吐 475.64 req/s(悲觀鎖同條件 664.07,-28.4%),
      平均延遲 3.8 倍。重試分佈最大 48 次、平均 7.04 —— 每賣一張票伴隨約 13 次註定失敗的嘗試。**
      重試上限對照(100 vs 10)判定:**上限不是吞吐的旋鈕**,兩組吞吐與成交數皆相同,
      只有延遲與失敗語意改變。新增兩條 ArchUnit 守則,其中一條把 CLAUDE.md 原本「無測試能抓到」的
      self-invocation Hard Rule 變成了測試
- [x] 8. `add-redis-prededuct-strategy` —— **完成。Phase 1 結束。**
      Redis 為准入控制、資料庫為最終帳本;Lua 腳本原子完成「限購 + 庫存 + 扣減」;
      非同步落庫走 Redis Stream + consumer group;即時補償 + 週期對帳。
      **回應改為 202 Accepted 且不含 `orderId`** —— 並更正了 spec 中
      「所有策略回應形狀完全相同」那條判準。`./mvnw verify` 綠,143 個測試。
      **零超賣、零超買、對帳差額 0、落庫收斂 ≤ 200ms。**
      **延遲是四層最低**(avg 266～443ms、p99 490～850ms,悲觀鎖為 761～867ms / 1.18s),
      但**吞吐與悲觀鎖無法分辨** —— 見下方「量測可信度」

**八組總表(每組獨立重啟、暖到收斂、量測 3 次;協定見第 11 支):**

> **本表由舊排程產生(純旋轉),已知有「先後固定」的偏差** —— 見第 16 支。
> 純旋轉均衡絕對位置但不均衡相對先後,某些配對會在每一輪都是同一方在前。
> **本批的水位是非單調的(5124 → 4871 → 4994),沒有強烈的系統性趨勢可供偏誤放大**,
> 但這是限制不是保證。日後重跑會自動使用均衡後的排程。

> **交錯量測,由 `k6/run-comparison-matrix.sh` 一道指令產生(可重跑)。**
> 一輪跑完八組、共三輪、每輪旋轉順序;每組獨立重建 app 與 postgres、暖到收斂、量測一次。
> **整批漂移 5.1%**(門檻 15%),水位 5124 → 4871 → 4994 非單調 —— **批次有效**。
> 取代第 11 支那份依序量測的舊表(舊表的虛擬四組全排在平台之後,有順序混淆)。

| 執行緒模型 | 策略 | req/s 中位數 | 全距 | 5xx | 售出 | 超賣 |
| --- | --- | --- | --- | --- | --- | --- |
| 平台 | noLock | 5118 | 4.1% | 0 | 50000 | **48933～49010** |
| 平台 | pessimistic | 4870 | 6.1% | 0 | 25000 | 0 |
| 平台 | optimistic | 2832 | 11.2% | 0 | 25000 | 0 |
| 平台 | redisPreDeduct | **24699** | 12.3% | 0 | 25000 | 0 |
| 虛擬 | noLock | 5466 | 3.8% | 0 | 50000 | **48942～49017** |
| 虛擬 | pessimistic | 4317 | 3.3% | 0 | 25000 | 0 |
| 虛擬 | optimistic | 1713 | 7.8% | 0 | 25000 | 0 |
| 虛擬 | redisPreDeduct | 23177 | 5.0% | 0 | 25000 | 0 |

**測量條件**:`availableProcessors: 4`、heap 1536MB、連線池 50、
**准入併發度上限:平台 200(Spring Boot 預設)/ 虛擬不受此上限約束**、對帳間隔 1000ms、
重試上限 100、postgres 2 核 / 1GB / `max_connections=500` / tmpfs、
redis:7 2 核 / 1GB(僅第 3 層使用)、k6 2 核 / 512MB、
1000 VU × 50 次 = 50000 個請求、初始配額 25000、限購上限 4、macOS + OrbStack。

> **准入上限那一欄是第 15 支回頭補註的。** 那個值在本表量測時就在作用,只是沒有被寫下來 ——
> **而它正是兩個執行緒模型最明顯的差別之一。** 一個沒進條件表的變數,
> 卻可能是差異的來源。第 15 支之後它由應用自報,不再靠人記得。

**正確性:八組全部成立。** 5xx 皆為 0;除刻意壞掉的第 0 層外超賣皆為 0;
Redis 預扣的對帳差額皆為 0。**這一半的成功標準已完全達成。**

**能下的效能結論:**

**① 樂觀鎖明確最差(兩個執行緒模型皆然)** —— 平台下比悲觀鎖低 **41.9%**
(2832 vs 4870,全距 11.2% / 6.1%),虛擬下低 **60.3%**(1713 vs 4317,全距 7.8% / 3.3%)。
搶票是樂觀鎖前提最不成立的場景。

**② Redis 預扣的受理速率明確最快** —— 平台下為悲觀鎖的 **5.07 倍**、虛擬下 **5.37 倍**。
**是受理速率不是完成速率**:回應時訂單還不存在,落庫收斂另計。

**③ 「虛擬執行緒的變異較大」被推翻。** 第 11 支量到虛擬四組全距 20.6%～53.1%、
平台四組 6.0%～8.9%,但那批是「平台四組跑完再跑虛擬四組」。
**交錯之後虛擬四組是 3.3%～7.8%,平台四組是 4.1%～12.3% —— 虛擬反而更穩定。**
那個 53.1% 是位置的產物,不是執行緒模型的性質。

**④ 虛擬 vs 平台:沒有一致的答案,它取決於那一層有沒有硬性序列化點。**

| 策略 | 平台 | 虛擬 | 差距 | 較不穩定方的全距 | 判定 |
| --- | --- | --- | --- | --- | --- |
| noLock | 5118 | 5466 | 虛擬**快 6.8%** | 4.1% | **虛擬略快** |
| pessimistic | 4870 | 4317 | 虛擬**慢 11.3%** | 6.1% | **虛擬較慢** |
| optimistic | 2832 | 1713 | 虛擬**慢 39.5%** | 11.2% | **虛擬明顯較慢** |
| redisPreDeduct | 24699 | 23177 | 虛擬慢 6.2% | 12.3% | **不得下結論** |

**唯一虛擬勝出的是無鎖層 —— 也是唯一沒有序列化點的一層。**

> **第 15 支已對此做了判別實驗(把平台的 `server.tomcat.threads.max` 200 → 1000)。
> 原本的推測「差異來自准入節流」只對了一半:**
>
> | 層 | 預測 | 實測 | 判定 |
> | --- | --- | --- | --- |
> | pessimistic | P1000 應變慢 | **慢 5.4%**(全距 2.7%) | **成立** |
> | noLock | P1000 應變快 | 名目慢 2.7%(全距 4.1%) | **不成立** |
>
> 而且**兩層都沒有因此重現虛擬的行為**:pessimistic 只走完 45% 的距離,
> noLock 反而離虛擬更遠。**准入併發度解釋了「有序列化點的層虛擬為何較慢」的一部分,
> 但完全不解釋「無序列化點的層虛擬為何較快」** —— 後者在兩個准入上限下都是 10.8%～13.9%。
>
> **該批同樣由舊排程產生**,而且 `P200_pessimistic` / `P1000_pessimistic` 正是
> 一個 3:0 的配對(第 16 支實測)。該批環境單調變熱,
> **但偏誤方向會讓量得較晚的 P1000 偏快,而它實測偏慢 —— 偏誤與結論相反,故判定保守。**
> 這是那批數據仍可用的理由,不是「影響應該不大」。

**不能下的結論(同等重要):**

**⑤ 無鎖 vs 悲觀鎖:平台執行緒下量不出來。** 差距 4.8%,而悲觀鎖的全距是 6.1% ——
**差距小於全距**。第 6 支量到 -16%、第 12 支量到 6.6%(亦小於全距),
**三批數據都無法支持一個明確的數字**。
但**虛擬執行緒下差距 21.0%、全距僅 3.8% / 3.3%,那一組結論明確**。

> **第 6～9 支記錄的效能數字已知不可靠**(未暖機、窗口過短)。以本表為準,
> 但那些 change 文件刻意不回頭改寫 —— 它們記載的是當時的認知,是學習軌跡。

**壓測 harness 刻意排在第 4 支而非最後:** 第 3 支完成後就有一個會超賣的版本,那正是 README 的開場數據。壓測能力若排在最後,第 6–8 支的驗收會缺少效能數字,而效能數字是每層驗收條件的一半。

## 修正

- [x] `fix-redis-key-expiry` —— **完成**。`purchased` 的期限繼承 `stock` 再加緩衝
- [x] `improve-load-test-fidelity` —— **完成**。暖機、多次量測、輸出附全距
- [x] `add-virtual-thread-comparison` —— **完成**(分支 `feat/add-virtual-thread-comparison`)。
      八組數據齊備、失敗率進入輸出。**本支真正的工作是三次量測協定的修正**:
      腳本靜默死掉、固定輪數的暖機必然低估、`TRUNCATE` 清不掉 WAL。
      **虛擬執行緒的問題仍未解答** —— 見下方待辦

- [x] `improve-measurement-ordering` —— **完成**(分支 `improve/improve-measurement-ordering`)。
      交錯量測(一輪跑完八組、共三輪、每輪旋轉順序)+ 批次自帶漂移指標與作廢門檻
      + 可重跑的編排腳本 `k6/run-comparison-matrix.sh`。未動任何應用程式碼,147 個測試維持綠。

      **本支的結論是 design D5 事先寫下的第三種結果:整批數據不可用(漂移 21.2%,門檻 15%)。**
      而依 D3,這個結果的價值在於它指出了下一步 —— 根因追到底了:
      **`pg_multixact` 累積到 679MB,佔掉 postgres 容器 1GB 上限的 68%**(見下方修正待辦)。

      **交錯本身確實生效**:`V_pessimistic` 全距從第 11 支的 **53.1%** 降到 **4.0%**,
      而高變異改由平台那幾組承擔(旋轉把它們排到了批次後段)。
      **「虛擬執行緒變異較大」幾乎確定是時間順序造成的** —— 但那要乾淨的一批才能定案

- [x] `fix-perf-db-multixact-accumulation` —— **完成。Phase 1 的成功標準全數達成。**
      (分支 `fix/fix-perf-db-multixact-accumulation`)。每組連 `postgres-perf` 一起重建
      (狀態隨容器消滅)+ 量測前的通用環境健康守則(判準取容器記憶體,
      不取 `pg_multixact` —— 前者是各類累積物共同的出口)。未動應用程式碼,147 個測試維持綠。

      **修正實測:批次結束時 `pg_multixact` 從 679M 降到 45M、容器記憶體 91.29% → 28.94%。**
      45M 正是最後一組自己的量 —— **跨組帶過來的是零**。
      整批漂移從 **21.2% 降到 5.1%**,八組中僅 2 組單調(期望值 2.7,即機率而非訊號)。

      **核心問題已回答:第 11 支「虛擬執行緒變異較大」的觀察被推翻** ——
      交錯後虛擬四組全距 3.3%～7.8%、平台四組 4.1%～12.3%,虛擬反而更穩定。
      **外鍵刻意保留**,那不是 bug。背景:
      `purchase_order.event_id REFERENCES event(id)` 讓每一筆 insert 都對**同一個** `event` 列
      取 `FOR KEY SHARE` 鎖;1000 併發同時持有 → PostgreSQL 配置 MultiXactId。
      24 次量測 = 120 萬筆 insert 全指向同一父列,`pg_multixact` 長到 **679MB**,
      而資料目錄在 **tmpfs(記憶體)**,直接吃掉容器的 1GB 上限。
      **`TRUNCATE` 清不掉、`CHECKPOINT` 清不掉(那是 WAL 的解)、`VACUUM FREEZE` 也清不掉**
      (截斷看 cluster 全域的 `datminmxid`,而 `template0` 的 `datallowconn=false` 碰不到)。
      **實測重建 postgres-perf 容器:679M → 16K、記憶體 91.29% → 17.95%。**
      修正動到 `run-load-test.sh` 與 compose,故不併入上一支

- [x] **交錯量測以分離「執行緒模型」與「時間順序」** —— **完成,Phase 1 收掉。**
      八組數據以交錯 + 旋轉重新取得,整批漂移 5.1%,由 `k6/run-comparison-matrix.sh` 一道指令產生。
      **第 11 支「虛擬執行緒變異較大」的觀察被推翻** —— 那是位置的產物

## Phase 1 之後的候選題目

- [x] `add-thread-admission-comparison` —— **完成**(分支 `feat/add-thread-admission-comparison`)。
      提高平台的 `server.tomcat.threads.max`(200 → 1000)作判別實驗,
      六組交錯三輪、漂移 3.4%。**兩個方向相反的預測只成立一個 —— 依 design D2 是「部分成立」。**

      **成立的那半:** pessimistic 放寬准入後**慢 5.4%**(全距 2.7%),方向與預測一致,
      且旋轉保留的相鄰關係加上本批單調變熱的趨勢**都對這個結論不利**,故判定保守。

      **不成立的那半:** noLock 名目慢 2.7% 而全距 4.1% —— **不得下結論**,
      而預測的是「變快」。

      **最重要的一點:提高准入在兩層都沒有重現虛擬的行為。**
      pessimistic 只縮短 45% 的距離,noLock 反而離虛擬更遠。
      **第 14 支那個推測作為完整解釋是錯的。**

      同時補上一個更基本的缺口:**Tomcat 的准入上限從來沒有進入測量條件** ——
      現由 `RuntimeInfoLogger` 讀 Tomcat 實際的 protocol handler 自報,
      虛擬執行緒下據實回報「不適用」而非那個已被賦值卻不生效的數字

- [ ] **無序列化點時,虛擬執行緒為何仍快 10.8%～13.9%?** —— 第 15 支排除了准入併發度:
      平台在 200 與 1000 兩個准入上限下都輸給虛擬同樣的幅度。
      **剩下的候選是虛擬執行緒本身在「大量並行阻塞 I/O」上的成本較低**
      (無執行緒池交接、掛起與恢復便宜、不佔用作業系統執行緒),
      但那還沒有被量過。可能的做法:量連線池等待時間的分佈,或比較 context switch 次數。
      **這是目前唯一還沒有解釋的數字**

- [ ] **第 15 支留下的干擾因素:「放寬准入」與「多 1000 條執行緒的成本」綁在一起** ——
      P1000 的容器記憶體比 P200 多約 20 個百分點(35% → 57%),那是執行緒堆疊。
      pessimistic 那 5.4% 有多少來自排隊、多少來自執行緒本身的成本,本批答不出來。
      要分離需要第三個組態(例如平台 500),或改用不增加執行緒數的節流方式

- [x] `fix-matrix-rotation-order-bias` —— **完成**(分支 `fix/fix-matrix-rotation-order-bias`)。
      **旋轉是保序的循環位移** —— 它均衡了絕對位置,卻完全沒有均衡相對先後。
      配對 (A,B) 的先後只取決於環繞點有沒有落在它們之間,落不到就每一輪都是 A 在前。

      **先更正我原本的用詞:缺陷是「順序固定」,不是「相鄰」。**
      相鄰本身無害 —— 兩組緊接著量測,只要三輪裡有時 A 先有時 B 先,趨勢就會抵銷。

      實測(六組三輪、偏移 {0,2,4}):**3 個配對是 3:0**,
      其中一個正是第 15 支的 `P200_pessimistic` / `P1000_pessimistic`。
      **而偏差的分佈不均 —— 哪些配對受影響取決於它們在清單裡的位置,與該比較重不重要無關。**

      修法:**偶數輪 = 前一輪的完整反序,奇數輪 = 正向旋轉**。
      反序翻轉所有配對,故第 1、2 輪必定各得一次,第 3 輪只能讓它成為 2:1 ——
      **對所有配對同時成立**。輪數為奇數時 2:1 即理論上界。
      `MATRIX_RESOLVE_ONLY` 現在會印出各輪排程、每組的絕對位置、逐配對的先後統計,
      並在失衡時回非零 —— **不需要跑量測就能驗**

## 需決定

- [ ] 前台框架(Vue 3 + Vite 或 Nuxt `ssr: false`)—— Phase 3 動工前決定,預設 Vue 3 + Vite。不論選哪個都不啟用 SSR
- [ ] 限購上限的實際張數 —— 暫定 4 張,壓測時可調整以觀察對競爭的影響

## 延後

- **README 素材:熱點父列 + 外鍵 = 一個無聲的可擴展性陷阱** ——
  `purchase_order.event_id REFERENCES event(id)`,而所有訂單都指向同一個場次。
  每一筆 insert 都要對那一列取 `FOR KEY SHARE`,1000 併發同時持有就會產生 **MultiXactId**;
  120 萬筆 insert 累積出 **679MB 的 `pg_multixact`**。
  **症狀是逐漸變慢,不是報錯** —— 沒有測試會紅,監控上只是「資料庫好像有點慢」,
  而 `TRUNCATE` / `CHECKPOINT` / `VACUUM FREEZE` 都清不掉它。
  **這是真實系統會踩到的東西,不是本專案的環境問題**(本專案只是碰巧把量放大到看得見)。
  寫 README 時應與「無鎖超賣」並列,作為第二個「只在夠大的量下才顯現」的例子

- **以場次結束時間取代「快取保存期限」** —— 目前的過期時間是自載入起算的固定期限,
  而正確的來源應該是場次的結束時間。schema 沒有那個欄位,為它新增 domain 欄位是
  Phase 1 排除的範圍蔓延。**已知的代價:銷售期若超過保存期限,場次會在銷售中變成「未開賣」**
  —— fail-closed 的停擺,不是資料錯誤。Phase 2 有場次狀態時處理

- **快取 TTL 的設定責任應移進應用** —— 目前由開賣的那一方(壓測腳本)負責,
  應用只能在對帳時警告。Phase 2 的管理介面把「開賣」變成應用內的動作後,
  TTL 應由應用自己保證,而不是靠警告

- **Redis 高可用(Sentinel / Cluster)** —— 第 8 支刻意不做:單節點足以呈現本層的併發特性,
  高可用是部署議題(Phase 4),混進來會讓本層的數據多出一個無關的變數。
  **Cluster 另有一個具體阻礙**:預扣的 Lua 腳本同時操作 `stock:*` 與 `purchased:*` 兩個 key,
  Cluster 要求同一支腳本的 key 落在同一個 slot —— 屆時必須改用 hash tag

- **訂單查詢端點** —— 第 3 層回 202 且不提供 `orderId`,客戶端只拿到冪等鍵。
  Phase 1 沒有查詢端點,所以那個鍵目前無處可用。**這是 202 這個決定尚未付清的代價**,
  Phase 2 補上以冪等鍵查詢訂單的端點

- **多實例部署時的 consumer name** —— 目前寫死 `consumer-1`。多實例共用同一個名稱會讓
  兩個實例共享 pending 清單、互相認領對方尚未處理完的訊息。部署前必須改為每實例唯一

- **重試退避(backoff)** —— 第 7 支刻意不做:重試風暴正是該層要量的現象,加退避會把它蓋掉。
  基準數據已取得(平均 7.04 次嘗試、最大 48 次、每張票伴隨約 13 次失敗嘗試)。
  **但第 7 支的上限對照顯示重試次數不是吞吐的瓶頸**(上限 100 與 10 的吞吐相同),
  因此退避能改善吞吐的機會不大 —— 若要做,目標應是延遲與資料庫負載,而不是吞吐

- **跨列鎖的順序** —— 目前只鎖單一庫存列,不會死鎖。若日後出現需要同時鎖多列的情境
  (例如跨場次套票),必須先定義鎖的取得順序,否則會死鎖

- **已取消訂單應排除於限購計算** —— 目前 `SUM(quantity)` 不篩選狀態。Phase 2 引入逾時取消後,
  已取消的訂單不應繼續佔用使用者的限購額度。現在只有 PENDING 狀態,該邏輯無法驗證,故不預先實作

- **Spring Security / 真實登入** —— Phase 2。橫切關注點,後加不需改架構;Phase 1 用 request header 帶 userId
- **訂單狀態機與逾時釋放庫存** —— Phase 2。逾時釋放是第三個併發考點(排程釋放與購買競爭同一列庫存)
- **Thymeleaf 管理介面** —— Phase 2,含策略即時切換與壓測儀表板
- **前台與 SSE 等候室** —— Phase 3,獨立 repo
- **部署與觀測(Micrometer / Prometheus / Grafana)** —— Phase 4

## Done

(尚無)
